modLoader = "kotlinforforge"
loaderVersion = "${kff_version_range}"
license = '${mod_license}'

[[mods]]
modId = "${mod_id}"
version = "${mod_version}"
displayName = "${mod_name}"
credits = "The teams for both Genetics Reborn as well as the original Advanced Genetics"
authors = "${mod_authors}"
description = '''${mod_description}'''
accessTransformers = "META-INF/accesstransformer.cfg"

[[dependencies.${ mod_id }]]
modId = "forge"
mandatory = true
versionRange = "${forge_version_range}"
ordering = "NONE"
side = "BOTH"

[[dependencies.${ mod_id }]]
modId = "minecraft"
type = "required"
versionRange = "${minecraft_version_range}"
ordering = "NONE"
side = "BOTH"

[[dependencies.${ mod_id }]]
modId = "kotlinforforge"
type = "required"
versionRange = "${kff_version_range}"
ordering = "NONE"
side = "BOTH"

[[dependencies.${ mod_id }]]
modId = "aaron"
type = "required"
versionRange = "${aaron_lib_version_range}"
ordering = "NONE"
side = "BOTH"

[[mixins]]
config = "geneticsresequenced.mixins.json"

# Features are specific properties of the game environment, that you may want to declare you require. This example declares
# that your mod requires GL version 3.2 or higher. Other features will be added. They are side aware so declaring this won't
# stop your mod loading on the server for example.
#[features.${mod_id}]
#openGLVersion="[3.2,)"
